name: Test

on:
  push:
    branches:
      - main
      - master
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-linux:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.12"
          enable-cache: false
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: x86_64
          args: --release --out dist
          sccache: true
          manylinux: auto
      - name: pytest
        shell: bash
        env:
          RUSTC_WRAPPER: ""
        run: |
          set -e
          uv sync --extra dev --find-links dist --reinstall
          uv run pytest

  test-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          architecture: x64
      - uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.12"
          enable-cache: false
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: x64
          args: --release --out dist -i python3.12
          sccache: true
      - name: pytest
        shell: bash
        env:
          RUSTC_WRAPPER: ""
        run: |
          set -e
          uv sync --extra dev --find-links dist --reinstall
          uv run pytest

  test-macos:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.12"
          enable-cache: false
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: aarch64
          args: --release --out dist
          sccache: true
      - name: pytest
        env:
          RUSTC_WRAPPER: ""
        run: |
          set -e
          uv sync --extra dev --find-links dist --reinstall
          uv run pytest

  benchmark:
    runs-on: ubuntu-latest
    needs: [test-linux]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.12"
          enable-cache: false
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: x86_64
          args: --release --out dist
          sccache: true
          manylinux: auto
      - name: Install dependencies
        run: uv sync --package benchmark --find-links dist --reinstall
      - name: Start Uvicorn server
        run: |
          uv run --package benchmark uvicorn benchmark.server:app --host 0.0.0.0 --port 8000 &
          sleep 10
      - name: Run benchmark
        run: uv run --package benchmark python benchmark/benchmark.py
